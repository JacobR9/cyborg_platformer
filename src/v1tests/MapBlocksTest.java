import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import java.awt.*;

import static org.junit.jupiter.api.Assertions.*;

class MapBlocksTest {
    /*
    //T4
    @Test
    @DisplayName("Confirms getMap() populates map and sets mapWidth > 0")
    void testGetMap(){
        Game game = new Game();
        game.loadImages();
        MapBlocks.map.clear();
        MapBlocks.mapWidth = 0;
        MapBlocks.getMap();
        assertFalse(MapBlocks.map.isEmpty(), "MapBlocks.map should contain tiles after getMap()");
        assertTrue(MapBlocks.mapWidth > 0, "Map width is not 0 or negative");
    }
    */
}
